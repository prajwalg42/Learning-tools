name: Secret testing

on: push

jobs:
  test-secrets:
    runs-on: ubuntu-latest

    steps:
      - name: Print a secret
        run: echo "My secret is $MY_SECRET"
        env:
          MY_SECRET: ${{ secrets.MY_SECRET }}
          
